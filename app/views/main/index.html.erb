<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <%= stylesheet_link_tag 'custom', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header>
    <h1>My Portfolio</h1>
    <%= link_to "ログアウト", logout_path, class: "button_logout", data: { "turbo-method": :delete } %>
    </header>
    <div class="toppage-container">
      <div class="profile">
        <div class="content">
          <div class="image">
            <% if @user&.image&.attached? %>
              <%= image_tag @user.image, class: 'profile-image' %>
            <% else %>
              <%= image_tag 'default_image.jpg', class: 'profile-default' %>
            <% end %>
          </div>
          <h5>自己紹介</h5>
          <div class="text">
          </div>
          <div class="input-group">
            <%= link_to "自己紹介を編集する", edit_user_path(@user), class: "button_myedit" %>
          </div>
        </div>
        <div class="avatar-name">
        </div>
      </div>
      <div class="chart">
        <div class="frame1">
          <h6>学習チャート</h6>
          <%= form_with(model: @user) do |f| %>
            <div class="input-group">
              <%= link_to "自己紹介を編集する", edit_skill_path(@user), class: "button_edit" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
    <footer>
      <p>portfolio site</p>
    </footer>
  </body>
</html>