<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <%= stylesheet_link_tag 'custom', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>
<body>
  <header>
    <h1>My Portfolio</h1>
    <%= link_to "ログアウト", logout_path, class: "button_logout", data: { "turbo-method": :delete } %>
  </header>
  <div class="login-container">
    <h2>スキル！！！！</h2>
    <div id="flash-messages">
      <% if flash[:error].blank? %>
        <% flash.each do |key, message| %>
          <div class="flash <%= key %>"><%= message %></div>
        <% end %>
      <% end %>
    </div>
    <div class="input-group">
      <%= form_with model: @user, url: user_path(@user), method: :patch, local: true, html: { multipart: true } do |form| %>
        <form action="main#index">
         <div>
            <span>
             <label for="introduction">自己紹介文</label>
              <input type="text" id="introduction">
            </span>
          </div>
          <span>
            <label for="image">アバター画像</label>
            <input type="file" id="image" name="画像ファイルを添付する">
          </span>
        </form>
        <%= form.submit "自己紹介を確定する", class: "button-edit" %>
      <% end %>
    </div>
  </div>
  <footer>
    <p>portfolio site</p>
  </footer>
</body>
</html>