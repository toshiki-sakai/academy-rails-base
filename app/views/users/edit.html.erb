<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <%= stylesheet_link_tag 'custom', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>
<body>
  <header>
    <h1>My Portfolio</h1>
    <%= link_to "ログアウト", logout_path, class: "button_logout", data: { "turbo-method": :delete } %>
  </header>
  <div class="login-container">
    <h2>自己紹介を編集する</h2>
    <div id="flash-messages">
      <% if flash[:error].blank? %>
        <% flash.each do |key, message| %>
          <div class="flash <%= key %>"><%= message %></div>
        <% end %>
      <% end %>
    </div>
    <div class="input-group">
      <form action="main#index">
        <div class="text-field">
          <span class="field-title">自己紹介文</span>
          <textarea id="introduction" rows="5" maxlength="200"></textarea>
          <span class="field-title">50文字以上、200文字以下で入力してください</span>
        </div>
        <div class="image-field">
          <span class="field-title">アバター画像</span>
          <label for="image">画像ファイルを添付する</label>
          <input type="file" id="image">
          <div class="file-name">
            <% if @user.image.present? %>
              <span class="select-image"><%= @user.image.filename %></span>
            <% else %>
              <span class="select-image">選択されていません</span>
            <% end %>
          </div>
        </div>
      </form>
      <%= link_to "自己紹介を確定する", root_path, class: "button-profile" %>
    </div>
  </div>
  <footer>
    <p>portfolio site</p>
  </footer>
</body>
</html>