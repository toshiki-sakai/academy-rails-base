<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <%= stylesheet_link_tag 'custom', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>
<body>
  <header>
    <h1>My Portfolio</h1>
    <%= link_to "ログインする", login_path, class: "button_white" %>
  </header>
  <div class="signup-container">
    <h2>新規登録</h2>
    <%= form_with(model: @user) do |f| %>
      <div class="input-group">
        <div class="input">
          <%= f.label :name, '氏名' %>
          <%= f.text_field :name, id: 'user_name', required: true %>
          <% if @user.errors[:name].any? %>
            <span class="error-message"><%= @user.errors[:name].first %></span>
          <% end %>
        </div>
        <div class="input">
          <%= f.label :email, 'メールアドレス' %>
          <%= f.email_field :email, id: 'user_email', required: true %>
          <% if @user.errors[:email].any? %>
            <span class="error-message"><%= @user.errors[:email].first %></span>
          <% end %>
        </div>
        <div class="input">
          <%= f.label :password, 'パスワード' %>
          <%= f.password_field :password, id: 'user_password', required: true %>
          <% if @user.errors[:password].any? %>
            <span class="error-message"><%= @user.errors[:password].first %></span>
          <% end %>
        </div>
        <%= f.submit "登録する", class: "button_blue" %>
      </div>
    <% end %>
  </div>
  <footer>
    <p>portfolio site</p>
  </footer>
</body>
</html>