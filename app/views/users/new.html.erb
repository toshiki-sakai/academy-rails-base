<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
</head>
<body>
  <header>
    <h1>My Portfolio</h1>
    <%= link_to "ログイン", login_path, class: "button_white" %>
  </header>
  <div class="signup-container">
    <h2>新規登録</h2>
    <%= form_with model: @user, local: true do |f| %>
      <div class="input-group">
        <div class="input">
          <%= f.label :name, '氏名' %>
          <%= f.text_field :name, id: 'user_name', required: true %>
          <% if @user.errors[:name].any? %>
            <span class="error-message"><%= @user.errors[:name].first %></span>
          <% end %>
        </div>
        <div class="input">
          <%= f.label :email, 'メールアドレス' %>
          <%= f.email_field :email, id: 'user_email', required: true %>
          <% if @user.errors[:email].any? %>
            <span class="error-message"><%= @user.errors[:email].first %></span>
          <% end %>
        </div>
        <div class="error-message">
        </div>
        <div class="input">
          <%= f.label :password, 'パスワード' %>
          <%= f.password_field :password, id: 'user_password', required: true %>
          <% if @user.errors[:password].any? %>
            <span class="error-message"><%= @user.errors[:password].first %></span>
          <% end %>
        </div>
      </div>
      <div class="button_blue_new_wrapper">
        <%= f.submit "登録する", class: "button_blue_new" %>
      </div>
    <% end %>
  </div>
  <footer>
    <p>portfolio site</p>
  </footer>
</body>
</html>